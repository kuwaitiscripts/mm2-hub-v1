local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "KUWAITI HUB V1 | يوسف هب 🔪",
   LoadingTitle = "جاري تشغيل نظام السيطرة...",
   KeySystem = true,
   KeySettings = {
      Title = "نظام الدخول | Key System",
      Subtitle = "ادخل المفتاح الخاص بالمطور يوسف",
      Note = "لا تسأل عن المفتاح، المطور فقط يعرفه",
      FileName = "KuwaitiHubKey", 
      SaveKey = true, 
      GrabKeyFromSite = false, 
      Key = {"kuwaitihub1"} -- المفتاح الذي طلبته
   }
})

-- 1. تبويب القتال (Combat) باستخدام الـ Remotes التي اصطدتها
local CombatTab = Window:CreateTab("القتال ⚔️", 4483362458)

CombatTab:CreateButton({
   Name = "رمي سكين تلقائي (Kill Target)",
   Callback = function()
       local target = nil
       local dist = math.huge
       for _, v in pairs(game.Players:GetPlayers()) do
           if v ~= game.Players.LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
               local d = (v.Character.HumanoidRootPart.Position - game.Players.LocalPlayer.Character.HumanoidRootPart.Position).Magnitude
               if d < dist then
                   dist = d
                   target = v
               end
           end
       end
       if target then
           local args = {
               [1] = target.Character.HumanoidRootPart.CFrame,
               [2] = target.Character.HumanoidRootPart.Position
           }
           game:GetService("Players").LocalPlayer.Character.Knife.Events.KnifeThrown:FireServer(unpack(args))
       end
   end,
})

CombatTab:CreateButton({
   Name = "إطلاق نار (Auto Shoot)",
   Callback = function()
       local target = nil
       for _, v in pairs(game.Players:GetPlayers()) do
           if v ~= game.Players.LocalPlayer and v.Character then
               target = v
               break 
           end
       end
       if target then
           local args = {
               [1] = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame,
               [2] = target.Character.HumanoidRootPart.Position
           }
           game:GetService("Players").LocalPlayer.Character.Gun.Shoot:FireServer(unpack(args))
       end
   end,
})

-- 2. تبويب الجوائز (Rewards)
local GiftTab = Window:CreateTab("الجوائز 🎁", 4483362458)

GiftTab:CreateButton({
   Name = "جلب جميع جوائز الباتل باس (Claim All)",
   Callback = function()
       Rayfield:Notify({Title = "يوسف هب", Content = "جاري جلب 100 جائزة...", Duration = 3})
       for i = 1, 100 do
           local args = {[1] = tostring(i)}
           game:GetService("ReplicatedStorage").Remotes.Events.Christmas2025Remotes.ClaimBattlePassReward:FireServer(unpack(args))
           if i % 10 == 0 then task.wait(0.1) end -- انتظار بسيط للحماية
       end
       Rayfield:Notify({Title = "تم", Content = "انتهت العملية!", Duration = 3})
   end,
})

-- 3. تبويب الرؤية (Visuals)
local VisualsTab = Window:CreateTab("رؤية 👁️", 4483362458)

VisualsTab:CreateButton({
   Name = "كشف القاتل والشرطي (ESP)",
   Callback = function()
       loadstring(game:HttpGet('https://raw.githubusercontent.com/Iratethisname10/CHUB/main/CHUBESP'))()
   end,
})

-- 4. إعدادات الواجهة
local ConfigTab = Window:CreateTab("الإعدادات ⚙️", 4483362458)
ConfigTab:CreateButton({
   Name = "إغلاق السكريبت",
   Callback = function() Rayfield:Destroy() end,
})

Rayfield:Notify({
   Title = "Kuwaiti Hub Ready!",
   Content = "المفتاح صحيح، استمتع باللعب يا بطل",
   Duration = 5,
})
